import 'package:flutter/material.dart';

class FavoriteStopsPage extends StatelessWidget {
  const FavoriteStopsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Favorite Stops'),
    );
  }
}
